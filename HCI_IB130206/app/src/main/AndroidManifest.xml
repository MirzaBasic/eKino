<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mirza.hci_ib130206">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".Helper.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/login"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".Movies.Activitys.MovieDetailsActivity"
            android:label="@string/title_activity_movie_details"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Movies.Activitys.MovieProjectionsActivity"
            android:label="@string/title_activity_movie_projections"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Movies.Activitys.MainMovieActivity"
            android:label="@string/title_activity_main_movie"
            android:theme="@style/AppTheme" />
        <activity android:name=".SlidingMenuActivity"></activity>
    </application>

</manifest>